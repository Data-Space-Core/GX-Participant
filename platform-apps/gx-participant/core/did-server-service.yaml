apiVersion: v1
kind: Service
metadata:
  name: gx-participant-did
  namespace: gx-participant
spec:
  selector:
    app.kubernetes.io/name: gx-participant-did
  ports:
    - name: http
      port: 80
      targetPort: 80
