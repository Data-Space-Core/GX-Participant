apiVersion: v1
kind: Service
metadata:
  name: gx-participant-minio
  namespace: gx-participant
spec:
  selector:
    app.kubernetes.io/name: gx-participant-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
